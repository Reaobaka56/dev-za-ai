# Backend API URL — set this in Vercel project settings
# For local dev it points to your local FastAPI server
# For production, point to your deployed API (Render, Railway, etc.)
VITE_API_BASE_URL=http://localhost:8000
